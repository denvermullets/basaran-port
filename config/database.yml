# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

cache: &cache
  <<: *default
  migrations_paths: db/cache_migrate

queue: &queue
  <<: *default
  migrations_paths: db/queue_migrate

development:
  <<: *default
  database: basaran_port_development

test:
  <<: *default
  database: basaran_port_test

production:
  primary:
    <<: *default
    url: <%= ENV['DATABASE_URL'] %>
    # username: basaran_port
    # password: <%= ENV["BASARAN_PORT_DATABASE_PASSWORD"] %>
  cache:
    <<: *cache
    url: <%= ENV['DATABASE_CACHE_URL'] %>
  queue:
    <<: *queue
    url: <%= ENV['DATABASE_QUEUE_URL'] %>
    migrations_paths: db/queue_migrate
  # cable:
  #   <<: *primary_production
  #   database: basaran_port_production_cable
  #   migrations_paths: db/cable_migrate
